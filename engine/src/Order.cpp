#include "orderbook/Order.hpp"

// Order implementation is fully inline in the header.
// This translation unit ensures the library has at least one compiled symbol
// and provides a place for future non-inline implementations.
